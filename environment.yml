
name: hypernets_processor
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7*
  #
  # dependencies (in alphabetical order)
  #
  - numpy >= 1.18.1
  - scipy >= 1.4.1
  - netcdf4 >= 1.5.3
  - xarray >=0.8.2
  #
  # for testing only
  #
  - pytest >=5.4.1
  - pytest-cov >=2.8.1
