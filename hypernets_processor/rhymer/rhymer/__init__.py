import os
import configparser

#from rhymer import ancillary
#from rhymer import processing
#from rhymer import Format
# from .shared.rhymer_shared import RhymerShared
#
# rs=RhymerShared()
#
# path = os.path.dirname(os.path.abspath(__file__))
#
# if not os.path.exists('{}{}config'.format(path, os.path.sep)):
#     path = os.path.split(path)[0]
#
# cfile = '{}{}data{}config.txt'.format(path, os.path.sep, os.path.sep)
# config = rs.config_read(cfile)
#
# ## test whether we can find the relative paths
# for t in config:
#     if os.path.exists(config[t]): continue
#     tmp = path + os.path.sep + config[t]
#     config[t] = os.path.abspath(tmp)

